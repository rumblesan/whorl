{
  "name": "whorl",
  "version": "0.1.0",
  "description": "A web based audio live coding environment",
  "main": "src/main.js",
  "author": "Guy John",
  "license": "BSD-3",
  "scripts": {
    "prebuild": "jison src/grammar/soundlang.jison -o src/jison-parser.js -m commonjs",
    "build": "browserify . > ./dist/app.js",
    "postbuild": "cp -r ./html/* ./dist/; cat ./style/*.css > ./dist/style.css;",
    "clean": "rm -rf ./dist/* src/jison-parser.js",
    "test": "nodeunit tests",
    "prepages": "npm run-script build",
    "pages": "git checkout gh-pages; cp ./dist/* .; git add app.js index.html style.css; git ci -m 'Updating pages'; git push origin gh-pages",
    "postpages": "git checkout master; git clean -dfx; git submodule update --init; npm install; npm run-script build"
  },
  "devDependencies": {
    "browserify": "^9.0.6",
    "jison": "^0.4.15",
    "nodeunit": "^0.9.1"
  }
}
